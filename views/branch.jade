extends layout

include mixins/assets

block content

  h1 branch

  mixin versionInfo(commit, nav)

  hr
  h3 assets

  table.table-bordered.table-hover.table-condensed
    each asset in assets
      tr
        td 
           a(href="/view/#{asset.xlink}") #{asset.name}

  a(href="/meta") metadata index